# 🍵 TELIET App

Ứng dụng đặt đồ uống, tích điểm DRIPS và quản lý tài khoản, xây dựng bằng **Expo + React Native + Firebase**.

---

## 🚀 Yêu cầu hệ thống

- Node.js >= 18
- npm hoặc yarn
- Expo CLI (`npm install -g expo-cli`)
- Tài khoản Firebase (để cấu hình API key)

---

## 📦 Cài đặt

1. **Clone repo**
   ```bash
   git clone https://github.com/your-username/drip-app.git
   cd drip-app
1. **Cài dependencies**
   npm install # hoặc yarn install